package main;

/**
 * Enum ENUM_BYTECODE. Contiene las instrucciones disponibles para el programa.
 * @author crisuroll 
 */
public enum ENUM_BYTECODE {
	PUSH, ADD, SUB, MUL, DIV, LOAD, STORE, OUT, HALT
}
